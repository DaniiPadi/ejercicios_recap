'use strict'

function sumarNum(num1, num2, num3) {
    let suma = num1 + num2 + num3;
    
    console.log(num1 + " + " + num2 + " + " + num3 + " = " + suma);
    
    return suma;
  }
  
  let resultado = sumarNum(19, 1, 9);
  console.log("El resultado de la suma es: " + resultado);